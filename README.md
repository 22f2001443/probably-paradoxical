# Probably Paradoxical

Repository layout:

- `frontend/` - Vue 3 + Vite public portal.
- `backend/` - reserved for future backend work.

Frontend commands:

```bash
cd frontend
npm install
npm run dev
npm run build
npm run preview
```
