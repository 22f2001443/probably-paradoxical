# Base URL of the backend API.
# Local dev defaults to the wrangler dev server, so this is optional locally.
# In production set it to your deployed Worker URL, e.g.
#   VITE_API_BASE_URL=https://backend.<your-subdomain>.workers.dev
VITE_API_BASE_URL=https://probably-paradoxical.samyaroy00.workers.dev
