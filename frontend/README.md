# Probably Paradoxical Frontend

Vue 3 + Vite portal for Probably Paradoxical at Paradox'26.

```bash
npm install
npm run dev
npm run build
npm run preview
```

All editable site content lives in `src/data/content.yml`.
